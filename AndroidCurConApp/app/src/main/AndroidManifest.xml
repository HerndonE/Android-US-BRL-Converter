<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="eherndon.csumb.currencyconverteroffline">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/curr"
        android:label="USDandBRL"
        android:roundIcon="@drawable/curr"
        android:supportsRtl="true"

        android:usesCleartextTraffic="true"

        android:theme="@style/Theme.CurrencyConverterOffline">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".usToBR">

        </activity>

        <activity android:name=".brToUSON">

        </activity>

        <activity android:name=".usToBRLON">

        </activity>

    </application>

</manifest>